"""dopest-clip — open-source, MCP-first, provider-agnostic media studio.

A fully self-contained package: record (OBS), edit (transcript-driven clipper),
process audio, and generate/edit images. Drivable by an MCP agent and by a human
in an Electron editor over the same project model and the same core operations.

This package has NO runtime dependency on any other project.
"""

__version__ = "0.1.0"
